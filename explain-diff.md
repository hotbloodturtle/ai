# explain-diff

> diff/브랜치/PR을 받아 배경·직관·코드 워크스루·퀴즈로 구성된 자체 포함 인터랙티브 HTML 설명서를 생성하는 프롬프트 (슬래시 커맨드)

- 원본: https://gist.github.com/geoffreylitt/a29df1b5f9865506e8952488eac3d524 (Geoffrey Litt)

## 소개

남이 만든 PR이나 큰 변경사항을 "깊게 이해"해야 할 때, diff를 눈으로 훑는 대신
읽을거리 형태의 설명 문서를 생성한다. 코드가 아니라 순수 프롬프트라 설치 의존성이 없다.

생성되는 HTML의 4개 섹션:

1. **배경(Background)** — 변경과 관련된 기존 시스템 설명. 초보자용 넓은 배경 + 변경 직결 좁은 배경
2. **직관(Intuition)** — 핵심 아이디어를 토이 데이터 예제와 HTML 다이어그램으로 설명
3. **코드(Code)** — 변경사항의 고수준 워크스루 (이해하기 쉬운 순서로 그룹화)
4. **퀴즈(Quiz)** — 중간 난이도 객관식 5문제, 클릭 시 정오답 피드백

## 특징

- 출력은 CSS/JS 포함 단일 HTML — 외부 의존성 없음, 폰에서도 열람 가능(반응형)
- 파일명이 `YYYY-MM-DD-` 로 시작해 시간순 정렬 + 버전 관리 밖(`/tmp`)에 저장
- ASCII 다이어그램 금지, HTML 기반 다이어그램 강제
- Martin Kleppmann 스타일의 명확한 문체 지향
- gist에는 Notion 페이지로 출력하는 변형(`explain-diff-notion.md`)도 있음 — Notion MCP 필요해서 미채택

## 설치

`~/.claude/commands/explain-diff.md` 에 프롬프트 저장 → 모든 프로젝트에서 슬래시 커맨드로 사용:

```
/explain-diff <브랜치명 | PR 번호 | "최근 커밋" 등 대상 지정>
```

## 원본 gist 대비 권장 수정 사항 (등록 시 적용)

- **퀴즈 결함 패치**: 커뮤니티 피드백에서 "정답이 항상 가장 긴 선택지"라 문제를 안 읽고도 맞힐 수 있다는
  지적이 있어, 선택지 길이를 비슷하게 + 정답 위치를 문제마다 다르게 하라는 지시 추가
- 생성 후 기본 브라우저로 자동 오픈 지시 추가
- 개인 스킬 `explain`(한국어)과 이름 충돌 없음 (`explain-diff`는 별개 커맨드)

## 팁

- 이 저장소처럼 문서 몇 줄짜리 diff에는 효용이 낮다. 큰 PR, 온보딩, 남의 브랜치 리뷰에 쓸 때 가치가 있다
- 대상을 명시하지 않으면 현재 워킹 트리/브랜치 기준으로 동작하므로, PR 번호나 커밋 범위를 함께 주는 게 정확하다
